메소드가 무엇이냐에 따라서 실행결과가 달ㄹ라짐
메소드
GET 조회
POST 추가
PUT 수정
DELETEE 삭제

다건, 단건 조회를 따로 할 시에는 Servlet을 두 개 생성해야함 = 하나의 Servlet에는 하나의 메소드만 가능
List = 다건 조회 = 와일드카드(?) 사용
객체 하나 = 단건 조회 = 키값을 검색(중복이 되지 않게


pool을 만드는 것 : 쓰레드를 만드는 것
동기화 - 쓰레드를 쓸 때 사용